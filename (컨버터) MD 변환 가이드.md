# LLM용 MD → HWPX 변환 가이드

> 이 문서는 LLM이 일반 마크다운 문서를 HWPX 변환용 마크다운으로 변환할 때 참고하는 규칙서입니다.

---

## 변환 목적

일반 마크다운(#, ##, -, \* 등)을 **md_to_hwpx.py 컨버터가 인식할 수 있는 형식**으로 변환합니다.

---

## 핵심 변환 규칙

### 1. 제목 변환

| 원본         | 변환 후         | 설명                   |
| ------------ | --------------- | ---------------------- |
| `# 제목`     | `<주제목> 제목` | 문서 최상위 제목       |
| `## 섹션`    | `□ 섹션`        | 대분류 소제목          |
| `### 소섹션` | ` ◦ 소섹션`     | 본문 (◦ 앞에 공백 1칸) |

### 2. 목록 변환

| 원본           | 변환 후          | 설명                     |
| -------------- | ---------------- | ------------------------ |
| `- 항목`       | `   - 항목`      | 설명2 (- 앞에 공백 3칸)  |
| `* 항목`       | `    * 항목`     | 설명3 (\* 앞에 공백 4칸) |
| `  - 중첩항목` | `    * 중첩항목` | 중첩은 설명3으로         |

### 3. 표 변환 (중요!)

**반드시 `<표 제목: ...>` 태그를 추가해야 표로 인식됩니다.**

```markdown
<!-- 원본 -->

| 항목 | 내용 |
| ---- | ---- |
| A    | B    |

<!-- 변환 후 -->

<표 제목: 항목 정보>
| 항목 | 내용 |
|------|------|
| A | B |
```

### 4. 강조 변환

| 원본             | 변환 후             | 설명               |
| ---------------- | ------------------- | ------------------ |
| `> 중요 내용`    | `<강조> 중요 내용`  | 인용문 → 강조 박스 |
| `**중요:** 내용` | `<강조> 중요: 내용` | 강조 문구          |

### 5. 볼드 유지

`**텍스트**` 형태의 볼드는 **그대로 유지**합니다. 컨버터가 자동으로 처리합니다.

---

## 공백 규칙 요약

```
□ 소제목          ← 공백 0칸
 ◦ 본문           ← 공백 1칸 + ◦
   - 설명2        ← 공백 3칸 + -
    * 설명3       ← 공백 4칸 + *
```

---

## 사용 가능한 마커

| 마커                            | 용도      | 대체 가능     |
| ------------------------------- | --------- | ------------- |
| `<주제목>`                      | 문서 제목 | -             |
| `□`                             | 소제목    | -             |
| `◦`                             | 본문      | `•`, `∙`, `·` |
| `-`                             | 설명2     | `–`, `—`      |
| `*`                             | 설명3     | `●`           |
| `<강조>`                        | 강조 박스 | -             |
| `<표 제목: ...>`                | 표 시작   | -             |
| `<요약표 시작>` ~ `<요약표 끝>` | 요약표    | -             |

---

## 변환 체크리스트

변환 시 다음 항목을 확인하세요:

- [ ] `#` 제목을 `<주제목>`으로 변환했는가?
- [ ] `##` 섹션을 `□`로 변환했는가?
- [ ] `###` 소섹션을 ` ◦`로 변환했는가? (공백 1칸)
- [ ] `-` 목록 앞에 공백 3칸을 추가했는가?
- [ ] `*` 목록 앞에 공백 4칸을 추가했는가?
- [ ] 모든 표에 `<표 제목: ...>` 태그를 추가했는가?
- [ ] 인용문/강조 문구를 `<강조>`로 변환했는가?
- [ ] 이모지를 모두 제거했는가? (hwpx 미지원)
- [ ] `**볼드**` 마크업은 그대로 유지했는가?
- [ ] `□`, `◦`, `-`, `*` 구분자 뒤에 구조 번호(1., 1.1, ① 등)를 붙이지 않았는가?

---

## 변환 예시

### 원본 마크다운

```markdown
# 2026년 사업 계획서

## 사업 개요

### 추진 배경

- 디지털 전환 가속화
- 산업 경쟁력 강화 필요

### 현황

| 항목 | 수치  |
| ---- | ----- |
| 예산 | 100억 |
| 인력 | 50명  |

> **핵심:** 2026년 내 완료 목표

## 세부 계획

- 1단계: 기획
  - 요구사항 분석
  - 설계
- 2단계: 실행
```

### 변환 후

```markdown
<주제목> 2026년 사업 계획서

□ 사업 개요

 ◦ 추진 배경

   - 디지털 전환 가속화
   - 산업 경쟁력 강화 필요

 ◦ 현황

<표 제목: 현황>
| 항목 | 수치 |
|------|------|
| 예산 | 100억 |
| 인력 | 50명 |

<강조> **핵심:** 2026년 내 완료 목표

□ 세부 계획

   - 1단계: 기획

    * 요구사항 분석
    * 설계

   - 2단계: 실행
```

---

## 주의사항

1. **이모지 금지**: hwpx에서 지원하지 않음 (✓, ❌, 📌 등 모두 제거)
2. **표 제목 필수**: `<표 제목: ...>` 없으면 표로 인식 안됨
3. **공백 정확히**: 공백 개수가 스타일을 결정함
4. **중첩 깊이 제한**: 최대 2단계 (설명2 → 설명3)까지만 지원
5. **빈 줄 유지**: 각 블록 사이에 빈 줄 1개 유지
6. **구분자 앞 숫자 금지**: `□`, `◦`, `-`, `*` 구분자 바로 뒤에 숫자 번호를 붙이지 않는다. 원본에 번호가 있어도 변환 시 제거할 것.
   - 잘못된 예: `□ 1. 사업 개요` / `   - 1단계: 기획`
   - 올바른 예: `□ 사업 개요` / `   - 1단계: 기획` (단, `-` 뒤 내용 자체가 "1단계"처럼 번호가 의미인 경우는 텍스트로 유지)
   - 구조 번호(1., 1.1, ①)는 제거, 내용 속 숫자("1단계", "3개월")는 유지

---

## 관련 파일

| 파일                         | 설명               |
| ---------------------------- | ------------------ |
| `style_textbook v1.md`       | 상세 스타일 규칙   |
| `templates/core_styles.yaml` | 스타일 정의 (YAML) |
| `converter/md_to_hwpx.py`    | 변환 스크립트      |

---

**버전:** 1.0.0 (2026-01-13)
