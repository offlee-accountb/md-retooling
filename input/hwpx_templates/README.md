Place HWPX templates here. Use `{{KEY}}` placeholders in `section*.xml` files for deterministic injection. Example placeholder: `{{TITLE}}`.

For the POC, run `python injector/exp_inject.py` — the script will create a minimal template if none exists.